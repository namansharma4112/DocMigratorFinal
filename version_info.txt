# UTF-8
#
# PyInstaller version resource for LegalDocMigration.exe
# -----------------------------------------------------------------------------
# WHY THIS FILE MATTERS FOR ANTIVIRUS:
# A PyInstaller .exe with NO company name, product name, version, description or
# copyright looks anonymous and "throwaway" to heuristic / machine-learning AV
# engines (like Trend Micro's TRX). Anonymous binaries score higher on
# suspicion. Giving the executable full, legitimate metadata makes it read like
# a normal corporate application and measurably reduces false positives.
#
# HOW IT IS USED:
# Place this file in the REPO ROOT (next to legal_migration.spec). The .spec
# already auto-detects it:  _version = "version_info.txt" if os.path.exists(...)
# so NO spec edit is needed — it is embedded automatically on the next build.
#
# MAINTENANCE: bump the four version numbers on each release (keep them in sync).
# -----------------------------------------------------------------------------

VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(1, 4, 0, 0),
    prodvers=(1, 4, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          u'040904B0',
          [
            StringStruct(u'CompanyName', u'Ardent Advisory & Accounting'),
            StringStruct(u'FileDescription', u'Legal Document Migration & Deduplication Tool'),
            StringStruct(u'FileVersion', u'1.4.0.0'),
            StringStruct(u'InternalName', u'LegalDocMigration'),
            StringStruct(u'LegalCopyright', u'Copyright (c) 2026 Ardent Advisory & Accounting. All rights reserved.'),
            StringStruct(u'OriginalFilename', u'LegalDocMigration.exe'),
            StringStruct(u'ProductName', u'Legal Document Migration Tool'),
            StringStruct(u'ProductVersion', u'1.4.0.0'),
            StringStruct(u'Comments', u'Internal document migration and deduplication utility. Author: Naman Sharma.')
          ]
        )
      ]
    ),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])])
  ]
)
